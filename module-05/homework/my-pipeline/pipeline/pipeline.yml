name: nyc_taxi
schedule: daily

start_date: "2026-02-20"

default_connections:
  duckdb: duckdb-default

variables:
  taxi_types:
    type: array
    items:
      type: string
    default: ["green", "yellow"]
